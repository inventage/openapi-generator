openapi: 3.0.2
info:
  title: Sample for unit tests with 'x-enumeration'
  version: '0.0.4'
  description: OpenAPI file containing a model with 'x-enumeration' objects
  x-short-name: test
servers:
  - url: https://localhost/api
paths: {}

components:

  schemas:

    SalutationTypeX:
      description: The salutation of a partner e.g. Mr, Mrs or Sir.
      type: string
      maxLength: 80
      x-enumeration: true

    Gender:
      type: string

    Address:
      type: object
      properties:
        street:
          type: string

    Response:
      description: The response object
      type: object
      properties:
        salutation:
          $ref: '#/components/schemas/SalutationTypeX'
        gender:
          allOf:
            - $ref: '#/components/schemas/Gender'
        address:
          $ref: '#/components/schemas/Address'
      required:
        - address
        - salutation
